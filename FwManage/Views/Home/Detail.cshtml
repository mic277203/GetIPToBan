@model List<FwManage.Helper.HightRequestPage>

@{
    ViewBag.Title = "明细";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<p>
    <a href="@Url.Action("Index","Home")"><strong class="text-info">返回</strong></a>
</p>
<table class="table table-striped table-bordered table-hover" style="word-break:break-all;word-wrap:break-all;">
    <thead>
        <tr>
            <th style="width:400px;">页面</th>
            <th>参数</th>
            <th style="width:100px;">请求数</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var m in Model)
        {
            <tr>
                <td>@m.Url</td>
                <td>@m.Param</td>
                <td>@m.Count</td>
            </tr>
        }
    </tbody>
</table>

